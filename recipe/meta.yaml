{% set name = "preact" %}
{% set version = "8.4.0" %}
{% set sha256 = "779ea451008140d784f9da05f9c0324e901dbe89d8018ad3311a97e2b233ea2a" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/developit/{{ name }}/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: generic
  script: npm install -g .

requirements:
  build:
    - nodejs
  
test:
  commands:
    - npm list -g preact
  requires:
    - nodejs
   

about:
  home: https://github.com/developit/preact
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Fast 3kb React alternative with the same ES6 API. Components & Virtual DOM.'
  description: |
    Fast 3kb React alternative with the same ES6 API. Components & Virtual DOM.   
  doc_url: https://preactjs.com/
  dev_url: https://github.com/developit/preact

extra:
  recipe-maintainers:
    - cshaley
    - sannykr
