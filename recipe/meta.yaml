{% set name = "preact" %}
{% set version = "10.0.5" %}
{% set sha256 = "608e1c747b2ce723ab21d616836c3c89e746bb3626ae12490eecb14aea90b378" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/developit/{{ name }}/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: generic
  script: npm install -g .

requirements:
  build:
    - nodejs
  
test:
  commands:
    - npm list -g preact
  requires:
    - nodejs
   

about:
  home: https://github.com/developit/preact
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Fast 3kb React alternative with the same ES6 API. Components & Virtual DOM.'
  description: |
    Fast 3kb React alternative with the same ES6 API. Components & Virtual DOM.   
  doc_url: https://preactjs.com/
  dev_url: https://github.com/developit/preact

extra:
  recipe-maintainers:
    - cshaley
    - sannykr
